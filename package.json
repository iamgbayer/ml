{
  "name": "ml",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel",
    "start:web": "yarn workspace @ml/web start",
    "build:web": "yarn workspace @ml/web build",
    "e2e": "yarn workspace @ml/e2e start",
    "unit": "yarn workspace @ml/web test --watchAll=false --coverage=true",
    "bootstrap": "lerna bootstrap"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.10.5"
  }
}